.gradle/
build/
*/build/
local.properties
# Per-user mise overrides (the shared config is mise.toml, which IS committed).
mise.local.toml
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/
reticle-report*/
# Default CLI screenshot output — ad-hoc captures shouldn't be committed.
/screenshot.png
# SwiftPM build output (e.g. the spikes/swift-host spike)
.build/
# SwiftPM/Xcode per-user state (xcuserdata etc.)
.swiftpm/

# Generated iOS sample Xcode project (source of truth is xcode/project.yml)
sample-app-ios/xcode/*.xcodeproj
