# Test Plan — ios-system-scope
Generated: 2026-08-11T10:40:00+08:00
Source: spec.md + plan.md

> 生成方式说明：本测试计划由主 agent 直接生成，未启动 test-design subagent（用户环境明确约束不使用 subagent）。双线框架与输出格式按 test-design 协议执行。
>
> 层次说明：本需求的可观测边界是 **CLI 命令的输出** 与 **真机上的实际状态变化**。因此绝大多数 TC 落在 UI/E2E 层（真机）；纯映射与纯判别逻辑落 UT 层。「API 层」在本项目对应 runner 的 HTTP 契约，标注为 API。

## Test Cases

### TC-001 齐备的设备一次准备后进入已安装状态并可跨会话复用 (US1-1)
- Given: 真机已连接、开发者模式开启、Enable UI Automation 已开启、设备已解锁；使用者提供的 team 在本机有含该设备的通配 profile
- When:
  1. 执行 `system prepare --team <id>`
  2. 记下输出中的设备标识
  3. 结束进程后重新执行一条 `system overlay`
- Then: 步骤 1 输出状态为「已安装」并含可复用的设备标识；步骤 3 成功且输出中不包含任何构建过程的迹象（不重新构建即可用）
- 来源: A线（AC展开）
- 测试层: UI/E2E
- 承载: [新增]

### TC-002 设备端 UI Automation 开关未开时报出该开关而非连接错误 (US1-2)
- Given: 真机各项就绪，但设备端 Settings → 开发者 → Enable UI Automation 处于关闭
- When: 执行 `system prepare --team <id>`
- Then: 命令失败；错误信息明确指出是设备端「Enable UI Automation」未开启且需在设备上手动开启；错误信息中**不出现** `exit 74`、`dtxproxy`、`channel refused`、`IDE disconnection` 等底层报错原文；命令不长时间挂起
- 来源: A线（AC展开 + 已知伪装报错，explore.md 记录）
- 测试层: UI/E2E
- 承载: [新增]

### TC-003 签名材料不可用时列出本机实际可用的 team (US1-3)
- Given: 使用者传入的 team id 在本机没有可用签名材料（无对应通配 profile 或 profile 不含该设备）
- When: 执行 `system prepare --team <不可用 id>`
- Then: 命令失败并说明是签名材料不可用；输出列出本机实际可用的 team 标识；设备上不产生任何新安装
- 来源: A线（AC展开）
- 测试层: UI/E2E
- 承载: [新增]

### TC-004 停止命令收掉常驻进程且不改变设备前台状态 (US1-4)
- Given: 设备上有一个存活的 runner；目标 app 正在前台显示某个已知界面
- When: 执行 `system stop`
- Then: 输出表明 runner 已退出、通道回到「已安装但未连通」；再查状态确认未连通；目标 app 仍在前台且界面未变
- 来源: A线（AC展开）
- 测试层: UI/E2E
- 承载: [新增]

### TC-005 旧版本 runner 被替换而非共存且替换被声明 (US1-5)
- Given: 设备上已安装一个与当前版本不同的 runner
- When: 执行 `system prepare --team <id>`
- Then: 输出声明发生了替换；设备上该 runner 的安装实例数为 1
- 来源: A线（边界：存量冲突）
- 测试层: UI/E2E
- 承载: [新增]

### TC-006 设备锁屏时准备命令立刻说明需解锁而不是超时 (US1-6)
- Given: 真机处于锁屏/熄屏状态
- When: 执行 `system prepare --team <id>`
- Then: 命令失败并说明设备需处于解锁状态；失败在短时间内返回，不出现长时间挂起后的超时报错
- 来源: A线（边界：设备状态）
- 测试层: UI/E2E
- 承载: [新增]

### TC-007 重复准备不产生第二个存活实例 (US1-7)
- Given: 设备上已有一个存活的 runner
- When: 再次执行 `system prepare --team <id>`
- Then: 命令成功；设备上该 runner 的存活进程数为 1
- 来源: A线（边界：并发/重复操作）
- 测试层: UI/E2E
- 承载: [新增]

### TC-008 对无存活实例的设备执行停止视为成功 (US1-8)
- Given: 设备上没有存活的 runner
- When: 执行 `system stop`
- Then: 命令成功退出；输出说明本无可停止的实例；不报错
- 来源: A线（边界：空操作幂等）
- 测试层: UI/E2E
- 承载: [新增]

### TC-009 系统权限弹窗的标题正文与每个可操作项均可读且带位置 (US2-1)
- Given: 目标 app 已触发系统通知权限弹窗，弹窗正压在 app 之上；system 通道已连通
- When: 执行 `system overlay`（不指定读取对象）
- Then: 输出包含弹窗标题文字、说明文字，以及「允许」「不允许」两个可操作项；每个可操作项带自己的位置与可否被点中；输出标明来源为 system 通道及作用进程；输出**不含**目标 app 自身界面的节点
- 来源: A线（AC展开）
- 测试层: UI/E2E
- 承载: [新增]

### TC-010 无覆盖物时明确说明而非返回空结果 (US2-2)
- Given: 目标 app 在最前面，没有任何系统界面压在它之上；system 通道已连通
- When: 执行 `system overlay`
- Then: 输出明确说明当前系统层没有覆盖物；输出不是一个需要使用者自行推断的空结果或空树
- 来源: A线（AC展开）
- 测试层: UI/E2E
- 承载: [新增]

### TC-011 显式指定主屏时返回主屏界面并在超限时声明截断 (US2-3)
- Given: 设备停在主屏；system 通道已连通；主屏节点数超出读取上限
- When: 执行 `system tree --target home`
- Then: 输出为主屏界面的节点；输出声明结果被截断、截断了多少、上限是多少；输出不呈现为一份完整的树
- 来源: A线（AC展开 + 边界：超限截断）
- 测试层: UI/E2E
- 承载: [新增]

### TC-012 该通道读不到的属性以不可读形式出现而非空值 (US2-4)
- Given: 已通过 `system overlay` 读到一个系统弹窗
- When: 查看输出中任一节点的属性
- Then: `isVisible`、样式类属性、区域划分、真实控件类名均以「此通道无法读取」的形式出现并附原因；这些属性**不以空值出现**；`hasFocus` 不作为字段出现（实测其在 web 内容树上恒为真，无判定价值）
- 来源: A线（AC展开）
- 测试层: UI/E2E
- 承载: [新增]

### TC-013 读取时界面消失则说明已不在而非返回过期内容 (US2-5)
- Given: 一个系统弹窗存在，但在读取动作发出后消失
- When: 执行 `system overlay`
- Then: 输出说明该界面已不在；输出不包含该弹窗的节点内容
- 来源: A线（边界：竞态）
- 测试层: UI/E2E
- 承载: [新增]

### TC-014 读取超时时归因于通道固有开销而非无限等待 (US2-6)
- Given: 读取对象的遍历耗时会超过约定上限
- When: 执行读取命令
- Then: 命令在上限处中止；输出说明是该通道逐属性跨进程读取的固有开销所致；命令不无限期等待
- 来源: A线（边界：耗时上限；explore.md 实测 126s）
- 测试层: UI/E2E
- 承载: [新增]

### TC-015 读取中 runner 消失则自动重试一次并声明曾中断 (US2-7)
- Given: system 通道已连通；读取过程中 runner 进程被终止
- When: 执行 `system overlay`
- Then: 该次读取自动重试一次；成功时输出正常结果**并标注 runner 曾中断并被重启**；重试仍失败时报错且同样说明曾中断过
- 来源: A线（边界：进程消失 + NFR-011）
- 测试层: UI/E2E
- 承载: [新增]

### TC-016 点击系统弹窗可操作项真实生效且记录经由哪条通道 (US3-1)
- Given: 目标 app 触发的系统通知权限弹窗正压在 app 之上
- When: 通过 `system tap` 点击「允许」
- Then: 弹窗关闭；目标 app 内表示授权结果的状态更新为已授权（以 app 自身可观测状态为证，而非仅命令返回成功）；输出记录本次动作经由 system 通道并标明作用进程
- 来源: A线（AC展开）
- 测试层: UI/E2E
- 承载: [新增]

### TC-017 回到主屏以 app 实际离开前台为证 (US3-2)
- Given: 目标 app 正在前台
- When: 执行 `system home`
- Then: 目标 app 不再处于前台状态；输出以 app 的实际状态变化为证据，而非仅报告指令已下发
- 来源: A线（AC展开）
- 测试层: UI/E2E
- 承载: [新增]

### TC-018 让 app 回到前台不重启它且保留原有界面状态 (US3-3)
- Given: 目标 app 曾在某个已知界面上产生了可观测的中间状态（如已填入的输入内容），随后离开前台
- When: 执行 `system activate` 让它回到前台
- Then: app 回到前台；该中间状态仍然存在（未被重启清空）；app 的进程标识与离开前台之前一致
- 来源: A线（AC展开 + NFR-005）
- 测试层: UI/E2E
- 承载: [新增]

### TC-019 系统界面上的坐标手势落在系统界面并标明作用进程 (US3-4)
- Given: 设备停在主屏；system 通道已连通
- When: 通过 `system tap` 在主屏上一个已知坐标做手势
- Then: 手势作用于系统界面（主屏仍在前台，未切走到目标 app）；输出标明作用进程为系统层而非目标 app
- 来源: A线（AC展开）
- 测试层: UI/E2E
- 承载: [新增]

### TC-020 指定的可操作项不存在时列出实际存在项而非静默无动作 (US3-5)
- Given: 一个系统弹窗存在，但其上没有名为某个指定标签的可操作项
- When: 通过 `system tap` 点击该不存在的标签
- Then: 命令失败；输出列出该界面上实际存在的可操作项；界面未发生任何变化
- 来源: A线（边界：目标缺失）
- 测试层: UI/E2E
- 承载: [新增]

### TC-021 越界坐标被拒绝执行 (US3-6)
- Given: system 通道已连通；屏幕尺寸已知
- When: 通过 `system tap` 指定一个超出屏幕范围的坐标
- Then: 命令拒绝执行并说明坐标越界；不派发任何手势
- 来源: A线（边界：非法输入）
- 测试层: UI/E2E
- 承载: [新增]

### TC-022 已下发但界面无变化时如实报告不表述为已生效 (US3-7)
- Given: 在系统界面上一处不会引发任何变化的位置
- When: 通过 `system tap` 在该处做手势
- Then: 输出报告「已下发但未观察到变化」；输出**不**将该结果表述为「已生效」或「成功」
- 来源: A线（边界：空效果；对应「只出证据不下判决」）
- 测试层: UI/E2E
- 承载: [新增]

### TC-023 显示级画面包含系统弹窗并标明取景范围 (US4-1)
- Given: 一个系统弹窗正压在目标 app 之上
- When: 执行 `system screenshot`
- Then: 得到的画面中可见该弹窗；输出标明该画面取自显示层，且与 app 进程内截图的取景范围不同
- 来源: A线（AC展开）
- 测试层: UI/E2E
- 承载: [新增]

### TC-024 两种截图各自标明来源使缺失可被正确归因 (US4-2)
- Given: 一个系统弹窗正压在目标 app 之上
- When:
  1. 执行 `ui screenshot`（app 进程内）
  2. 执行 `system screenshot`（显示级）
- Then: 两次输出各自标明来源与取景范围；进程内截图中弹窗不可见且其输出声明了该取景范围的已知缺失；使用者据此可区分内容缺失是「app 没画」还是「该通道拍不到」；`ui screenshot` 既有的来源与降级取值未因本次改动而改变
- 来源: A线（AC展开 + 回归既有输出）
- 测试层: UI/E2E
- 承载: [新增]

### TC-025 熄屏时说明屏幕未点亮而非返回纯黑图 (US4-3)
- Given: 设备屏幕处于熄屏状态
- When: 执行 `system screenshot`
- Then: 输出说明屏幕未点亮；不返回一张会被误判为界面异常的纯黑图
- 来源: A线（边界：设备状态）
- 测试层: UI/E2E
- 承载: [新增]

### TC-026 进程内坐标够不到时仍拒绝且给出两种可能与通道指引 (US5-1)
- Given: 真机上目标 app 已连通 linked agent；指定一个不在该 app 任何窗口内的坐标
- When: 执行 `act tap --point <该坐标>`
- Then: 命令拒绝且不产生任何点击（行为与改动前一致）；错误信息同时说明两种可能——坐标不正确、或目标在另一进程；并指出后者应改用 system 通道
- 来源: A线（AC展开）
- 测试层: UI/E2E
- 承载: [复用] `scripts/e2e-ios-device.sh` COORDINATE GESTURES 段可承载拒绝路径的回归，文案断言需新增

### TC-027 system 通道不可用时错误信息区分未安装与未连通 (US5-2)
- Given: 真机上指定一个不在目标 app 任何窗口内的坐标
- When:
  1. 在 system 通道**尚未安装**的设备上执行 `act tap --point <该坐标>`
  2. 在**已安装但未连通**的设备上执行同一命令
- Then: 两次均拒绝且均说明上述两种可能；步骤 1 的输出指引「需先准备」，步骤 2 的输出指引「只需启动」；两条指引不相同
- 来源: A线（边界：通道状态分支）
- 测试层: UI/E2E
- 承载: [新增]

### TC-028 模拟器上请求 system 能力时指引改用现有命令
- Given: 目标为 iOS 模拟器
- When: 执行任一 `system` 命令
- Then: 命令说明该场景已被现有通道覆盖，并指出应改用哪条现有命令；不尝试在模拟器上安装或启动 runner
- 来源: B线（system 命令族的平台准入分支；改变对外响应）
- 测试层: UI/E2E
- 承载: [新增]

### TC-029 runner 与目标 app 端口冲突在准备阶段被拒绝而非静默复用
- Given: 构造一种 runner 与目标 app 的 bundle id 落到同一派生端口的情形
- When: 执行 `system prepare --team <id>`
- Then: 命令失败并说明端口冲突；不建立两条抢占同一端口的隧道
- 来源: B线（端口派生与隧道建立路径；影响外部系统交互，且冲突会表现为难以归因的间歇失败）
- 测试层: UT
- 承载: [新增]

### TC-030 ElementType 到 role 的映射对未知取值不静默丢弃
- Given: 一个当前映射表未覆盖的元素类型取值
- When: 将该取值映射为 role
- Then: 产出一个明确表示「未识别类型」的 role 并保留原始取值，便于归因；不静默丢弃该节点，也不误映射为某个已知 role
- 来源: B线（SystemNode 类型映射；影响对外响应内容）
- 测试层: UT
- 承载: [新增]

### TC-031 UI Automation 开关未开的失败形态被判别为该原因而非通用连接失败
- Given: 一组代表性的启动失败形态输入，其中包含「开关未开」特征与其它连接失败特征
- When: 对每种形态执行失败原因判别
- Then: 「开关未开」形态被判别为该原因并给出设备端操作指引；其它形态不被误判为该原因
- 来源: B线（Lifecycle 失败判别路径；直接决定对外错误信息，且已知底层报错会伪装，是本需求最易劣化为玄学报错之处）
- 测试层: UT
- 承载: [新增]

---

## 统计

- TC 总数: 31（A线: 27，B线: 4）
- 层次分布: UI/E2E 28、UT 3
- 回流项: 0
